# This directory holds per-project infrastructure config:
#   - mushin.caddy   (generated by /start-new-app from caddy-config/template.caddy)
#   - mushin.service (generated by /start-new-app from systemd-service/template.service)
# Both are installed to /etc/ on the server by bootstrap-project.sh.
